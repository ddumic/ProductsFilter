namespace ProductsFilter.Model.Common
{
    /// <summary>
    /// Constraint interface that must be implemented by each domain entity that has it's own repository and can exist by itself
    /// </summary>
    public interface IAgregateRoot
    {
    }
}
